# Obsidian 项目协作规则

处理软件开发任务前，必须先用 `skill` 工具加载 `obsidian-project-management`（即 `$obsidian-project-management`），并以 `E:\obsidian_warehouse` 为唯一项目记录。

开始实现前，读取当前项目的 `SPEC.md`、`任务计划.md` 和目标任务文件；完成、暂停或交接前，更新任务进度与验证证据，并创建或更新 handoff。范围变化必须先写入 `SPEC.md`，不得静默偏离。

以上仅适用于需要持续跟踪的软件开发任务；一次性问答、题外话、与仓库无关的临时脚本不触发、不写入 vault。
