# 项目记录规则

处理软件开发任务前，必须先用 `skill` 工具加载 `obsidian-project-management`（即 `$obsidian-project-management`）。

**记录位置：Vault**——`<Vault 根>\软件开发\Project OS（Obsidian迁移）\`（Vault 根见技能目录 `config.json` 的 `vaultRoot` 字段）。理由：本仓库是公开仓库，开发过程记录含本机路径与过程细节，不入库；`.gitignore` 忽略 `records/` 与 `config.json` 作为兜底。记录位置以本文件声明为准。

开始实现前读该项目的 `SPEC.md`、`任务计划.md` 与在途变更；完成、暂停或交接前更新进度、写验证证据与 handoff；范围变化先写 `SPEC.md`，不得静默偏离。

以上仅适用于需要持续跟踪的软件开发任务；一次性问答、题外话、与仓库无关的临时脚本不触发、不写盘。
